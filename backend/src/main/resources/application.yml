spring:
  application:
    name: barbearia-api

  datasource:
    # url: ${DB_URL:jdbc:postgresql://172.16.44.208:5432/barbearia}
    url: ${DB_URL:jdbc:postgresql://localhost/barbearia}
    driver-class-name: ${DB_DRIVER_CLASS:org.postgresql.Driver}
    username: ${DB_USERNAME:postgres}
    # password: ${DB_PASSWORD:123}
    password: ${DB_PASSWORD:root}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        jdbc:
          time_zone: "America/Sao_Paulo"

  jackson:
    time-zone: "America/Sao_Paulo"
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false

  flyway:
    enabled: true

  ldap:
    # urls: ${LDAP_URL:ldap://172.16.38.168:389}
    urls: ${LDAP_URL:ldap://10.228.64.168:389}
    base: ${LDAP_BASE:ou=contas,dc=fab,dc=intraer}

api:
  secret:
    token:
      secret: ${JWT_SECRET:dev-secret}

webservice:
  api-url: ${WEBSERVICE_API_URL:http://webservice.ccabr.intraer/api/}
  username: ${WEBSERVICE_USERNAME:barbearia}
  password: "${WEBSERVICE_PASSWORD:B@rber$h0p}"

# server:
#   port: ${SERVER_PORT:8081}
